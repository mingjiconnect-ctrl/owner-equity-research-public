# Phase 5E market-execution policy

Phase 5E-0 defines policy and immutable internal receipt shapes. Phase 5E-1.1 adds an internal,
explicit access gate that strictly reloads the price-blind artifact, selects the current v4
authorization, replays an evidence-bound security identity, owns both timestamps, and invokes the
component-locked exact Provider exactly once. The Provider returns raw bytes only; the locked
parser and explicit 2026 XNYS/XNAS dataset are the sole interpretation and calendar authorities.
Neither phase exposes a market client, access operation, Snapshot builder, complete-request
compiler, kernel invocation, or writer to this Skill.

An explicit future market request must begin after the exact `market_reference_allowed` Handoff,
use the component-locked provider, the latest completed regular-session official unadjusted close on or
before cutoff, one primary common security in reporting currency, quote-date current common shares
shares, and split factor one. Unsupported security or split structures fail closed.

The final request must preserve existing Fact bytes, assumption-entry bytes, the price-blind
artifact, and both protected method hashes. The pinned kernel will execute only from a verified
wheel in a network-disabled subprocess. This Skill must never trigger any of those operations
implicitly.

Invalid responses are represented only by a response hash and registered reason codes; raw bytes
are not returned or written by the research core. Phase 5E-2A accepts the validation-only
`MarketReferenceSnapshot 2.0.0` contract under audit `2.3.2`; Phase 5E-2A.1 closed its
dilution-root and decimal-domain semantics under audit `2.3.2.1`. Phase 5E-2A.2 pins rc.2 and
upgrades the sole Snapshot contract to v3 current-common-share semantics under audit `2.3.2.2`.
It has no builder and is accepted/closed. Phase 5E-2A.2.1 adds recursive raw-root, cutoff-safe
source, corporate-action search-coverage, and completed-claim transition validation under audit
`2.3.2.2.1` and is accepted/closed. Phase 5E-2B has historical governance evidence under audit
`2.3.2.3`, but Phase 5E-2B.1 must close cross-source legal-event identity before market evidence.
Production grouping is accepted under audit `2.3.2.3.2`. Phase 5E-2B.1-2A now defines only the
immutable exactly-once integration contracts under audit `2.3.2.3.3`, including exact
one-entry-per-category coverage, category/security-specific N/A human review chains, duplicate
typed-evidence rejection, direct-Fact and cutoff-safe proof, graph-byte-bound coverage/transition
objects, specialist deferral for convertible/warrant transitions, exact source-byte code identity,
independent test replay, official-occurrence collision detection, official/high opening roots,
multi-root-Claim blocking, generated-ID reservations, exact one-chain transitions, and a
credential-partitioned gate whose candidate execution is secret-isolated and whose introduction is
independently reviewed before later base-owned use. It
exposes no executable surface. Before the validated two-file closeout exists, only acceptance is
authorized and 2B is prohibited; afterward only `feature/phase5e2b12b-canonical-rollforward` may
change compiler, dedicated test, and state under the preinstalled frozen 2B gate. Phase
5E-2B.1-2C and Phase 5E-2C through 2F remain prohibited in both states.

After legacy S3, the protected-base controller advances only through recursive G1, G2, G3, G4,
and G5: inert gate, accepted gate, successor pending acceptance, accepted successor, then total
closeout and the exact next inert seed. Dynamic audit profiles follow the deepest validated gate;
candidate oracle text is inert. `Phase 5E-2B.1-2C` is current-share recursive closure and is not
the later `Phase 5E-2C` exact market-evidence phase. Phase 6 through Phase 9 are outside this map.
The current GitHub Free private repository has no pinned Controller App, kernel-only read-only
Kernel Reader App, or protected environments,
so remote acceptance remains prohibited.

Phase 5 current authority: S3 -> G1 -> G2 -> G3 -> G4 -> G5 -> external 2C-P; after feasibility a new protected gate is required; Phase 6-9 require separate reviewed control-plane authorization; Phase 5E-2B.1-2C != Phase 5E-2C.
